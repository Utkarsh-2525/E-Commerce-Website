import { BudgetKartValidators } from './budget-kart-validators';

describe('BudgetKartValidators', () => {
  it('should create an instance', () => {
    expect(new BudgetKartValidators()).toBeTruthy();
  });
});
